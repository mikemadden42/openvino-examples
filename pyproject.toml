[project]
name = "openvino-examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openvino>=2025.4.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
